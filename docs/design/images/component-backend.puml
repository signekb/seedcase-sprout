@startuml component-backend
!theme C4_green from <C4/themes>
!include  https://raw.githubusercontent.com/plantuml-stdlib/C4-PlantUML/master/C4_Component.puml

' C4 Component diagram for backend database and file storage.

Container(management, "Management containers")

Container_Boundary(bee, "(Meta)data storage") {
  Container_Boundary(rdb_metadata, "Relational database for metadata") {
    Component(changelog, "Changelog of data", $descr="Listing changes made to the (meta)data")
    Component(metadata, "Metadata on data", $descr="Details like data types, column names, and data collection or generation procedure")
  }
  Component(users, "Database for user details", $descr="Details about user, roles, and connected projects")
  Component(projects, "Database for project details", $descr="Details about projects, list  uploaded for project entered into database")
  Container_Boundary(data_storage, "Storage for project data") {
    Component(data, "Relational database for data", $descr="Data uploaded for project entered into database")
    Component(raw_file, "File storage", $descr="To keep a copy of the raw data that is saved to the database")
  }
}

Lay_Right(rdb_metadata, users)
Lay_Right(data_storage, projects)
Lay_Down(rdb_metadata, data_storage)

Rel(management, data_storage, "Write")

Rel(management, projects, "Read  and  write")
Rel(management, users, "Read  and  write")

Rel(management, metadata, "Read  and  write")
BiRel(management, changelog, "Auto-generated")

@enduml
